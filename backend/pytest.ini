[pytest]
testpaths = tests blue_team_v2/tests ml/tests
python_files = test_*.py
markers =
    live: requires the Docker data stack (Neo4j/Postgres/Redis/BELS) — skipped by default
addopts = -q -m "not live"
filterwarnings =
    ignore::DeprecationWarning
