"""TGIE graph layer (Phase 2+): persistent Neo4j knowledge graph + schema.

Coexists with the legacy in-memory `graph_engine` package, which becomes a hot
cache / live-demo projection of this graph in Phase 3.
"""
