# BELS runtime dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9   # multipart form uploads
reportlab>=4.0            # PDF forensic reports (optional; graceful fallback if absent)
cryptography>=42.0        # Ed25519 signatures (optional; HMAC fallback if absent)
# web3>=6.0               # ONLY needed when BELS_PROVIDER=ethereum
